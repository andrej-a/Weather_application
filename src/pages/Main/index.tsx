import React from 'react';

import { Wrapper } from './styles';

const Main = () => {
    return (
        <React.Fragment>
            <Wrapper />
        </React.Fragment>
    );
};

export default Main;
